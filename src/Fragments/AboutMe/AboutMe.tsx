import styles from './AboutMe.module.scss'
import cn from 'classnames'

const AboutMe: React.FC = () => {
  return <section className={cn(styles.content)}>About Me</section>
}

export default AboutMe
